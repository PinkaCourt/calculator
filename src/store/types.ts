export type User = {
  accessToken: string;
  auth: boolean;
  avatar: null;
  email: string;
  name: null;
};

export type Data = {
  id: number;
  ds: string;
  ans: string;
  wtl: string;
  userId: string;
  date: Date;
};
